{%extends 'baseBack.html.twig'%}

{% block body%}
    <ul class="list-group">
        {%for user in users%}
            <li class="list-group-item">
                <a href="{{path('user_edit',{id:user.id})}}">
                    {{user.username}}
                </a>
            </li>
        {%endfor%}
    </ul>
{%endblock%}