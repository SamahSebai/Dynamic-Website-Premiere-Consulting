{#
{% extends 'base.html.twig' %}

{% block title %}Newsletter{% endblock %}

{% block body %}
    <h1>Newsletter</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ newsletter.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ newsletter.date ? newsletter.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ newsletter.contenu }}</td>
            </tr>
            <tr>
                <th>Objet</th>
                <td>{{ newsletter.objet }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('newsletter') }}">back to list</a>

    <a href="{{ path('newsletter_edit', {'id': newsletter.id}) }}">edit</a>

    {{ include('newsletter/_delete_form.html.twig') }}
{% endblock %}
#}
<html>
<body>
<h1>{{newsletter.objet}}</h1>
<p>{{ newsletter.contenu | raw }}</p>
</body>
</html>
